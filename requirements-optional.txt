# Optional PC-side training, evaluation and asset tooling.
# The board runtime uses the deployed RKNN CLI/runtime instead of these wheels.
numpy>=1.23
onnxruntime>=1.16
opencv-python>=4.8
ultralytics>=8.0
edge-tts>=6.1
imageio-ffmpeg>=0.4
